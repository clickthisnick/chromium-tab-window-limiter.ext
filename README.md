# Simple Chromium Tab/Window Limiter

## To Install

Chrome -> Settings -> Extensions -> Toggle Developer Mode -> Load Unpacked -> Load the `src` directory of this repo.

## Make better

You can hide extensions in Chrome
